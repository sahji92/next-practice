import React from 'react'

export default function login() {
  return (
    <div>login</div>
  )
}
//add parenthesis() in routes group name(auth)
 //so that group name is omitted from the url while routing.
