import React from 'react'

export default function PrivateFolder() {
  return (
    <div>private folders are not considered by routing system, 
        add underscore at prefix of folder name to
        make it non-routable
    </div>
  )
}
